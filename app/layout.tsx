import type {Metadata} from "next";import "./globals.css";\nimport PwaRegister from "../components/PwaRegister";export const metadata:Metadata={title:"ZED Gift Shop — Gifts That Say More.",description:"Premium personalized gifting across Kenya.",applicationName:"ZED Gift Shop",manifest:"/manifest.webmanifest",themeColor:"#063121"};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><PwaRegister />{children}</body></html>}